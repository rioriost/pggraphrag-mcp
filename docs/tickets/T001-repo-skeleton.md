# T001 Repository skeleton

## Goal
Create the initial repository structure for `pggraphrag-mcp`.

## Tasks
- Create top-level directories: `docker`, `scripts`, `schemas`, `src`, `tests`, `docs`
- Create Python package `src/pggraphrag_mcp`
- Add `README.md`, `.env.example`, `.env.production.example`, `pyproject.toml`
- Add placeholder Makefile targets for build, test, smoke, and compose up/down

## Done when
- Repository tree exists and lint/test command placeholders run without failing due to missing paths
