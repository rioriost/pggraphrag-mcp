# T008 MCP tools and resources schema

## Goal
Define the exposed MCP contract.

## Tasks
- Add tool schemas for health/index/graph status
- Add resource URI patterns for document, chunk, entity, retrieval trace
- Add shared response envelopes
- Version tool schemas

## Done when
- Tool discovery returns stable machine-readable schemas
