# T003 Environment and secrets

## Goal
Define naming and secret handling conventions.

## Tasks
- Add `.env.example` and `.env.production.example`
- Use `PGGRAPHRAG_MCP_` prefix consistently
- Document which values are generated placeholders
- Add helper script for populating local placeholders

## Done when
- A developer can copy the example env file and identify every required setting
