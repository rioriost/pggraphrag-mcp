# T006 AGE bootstrap and refresh

## Goal
Provide explicit operational scripts for graph lifecycle.

## Tasks
- Implement `ensure_age_extension.py`
- Implement `bootstrap_graph.py`
- Implement `refresh_graph.py`
- Make scripts idempotent where possible

## Done when
- Running bootstrap twice does not corrupt graph state
